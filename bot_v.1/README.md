# Grocery Bot

This directory is the bot-local root used by Codex.

Local here:
- source files
- tests
- configs
- best
- runs
- bot scripts

Global one level above:
- ../experiments
- ../shared
- ../agents
- ../archive

Path-aware bot scripts resolve the lab root via `../.lab_root` or the `GROCERY_LAB_ROOT` override.

# Grocery Bot Lab

This is the shared lab root.

- Bot-local code stays inside bot folders.
- Cross-agent collaboration lives in `shared/`.
- Experiment records live in `experiments/`.
- Reversible migrations and deprecated material live in `archive/`.

Canonical bot selection is tracked in `shared/shared_state/canonical_bot.yaml`.
